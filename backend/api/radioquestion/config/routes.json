{
  "routes": [
    {
      "method": "GET",
      "path": "/radioquestions",
      "handler": "Radioquestion.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/radioquestions/count",
      "handler": "Radioquestion.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/radioquestions/:id",
      "handler": "Radioquestion.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/radioquestions",
      "handler": "Radioquestion.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/radioquestions/:id",
      "handler": "Radioquestion.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/radioquestions/:id",
      "handler": "Radioquestion.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
