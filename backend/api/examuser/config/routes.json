{
  "routes": [
    {
      "method": "GET",
      "path": "/examusers",
      "handler": "Examuser.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/examusers/count",
      "handler": "Examuser.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/examusers/:id",
      "handler": "Examuser.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/examusers",
      "handler": "Examuser.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/examusers/:id",
      "handler": "Examuser.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/examusers/:id",
      "handler": "Examuser.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
